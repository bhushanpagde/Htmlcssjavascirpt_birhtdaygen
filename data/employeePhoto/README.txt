Employee photos selected in the Birthday Cards page are saved here when the user grants browser access to the project folder.

Photos are also retained in the browser's IndexedDB storage so the standalone app works without folder permission.
